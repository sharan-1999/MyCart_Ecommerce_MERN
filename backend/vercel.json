{
    "version":2,
    "builds": [
        {"src": "*.js", "use": "@vercel/node", "config": {"outputDirectory": "public"}}
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}